# 2026-08-09

- Josh asked Jen to ingest the Starter Template changelog and fold it into platform understanding.
- Reviewed `import/unity6-docs/.claude/CHANGELOG.md`.
- Important durable takeaways from the changelog:
  - Confirms the implemented web-first Supabase auth model with shared `.metadyn.xyz` cookie, Unity WebGL bridge, profile fetch, and persisted `avatar_index` continuity.
  - Confirms Hyperfy unified login should be treated as already working; follow-up work is profile continuity across surfaces, not first-time SSO invention.
  - Confirms the current deployment pattern is environment/profile-based rather than assuming bare-root deploys, with generated room/space path suffix behavior documented.
  - Confirms the starter-template framing: client onboarding should start from the existing Pavilion + MetaDyn SDK codebase, with MetaDyn-hosted subdomains first and custom-domain auth handoff treated as a later step unless explicitly built.
  - Confirms the older dynamic avatar-selection milestone, but this should not override the newer 2026-08 canonical runtime-avatar plan and remaining hardening work.
- Observed that the imported changelog appears to stop at 2026-03-16, so it is useful historical context but not the sole source of current platform state; newer canonical planning docs and Josh’s later corrections remain higher priority.

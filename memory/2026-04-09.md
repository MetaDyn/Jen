# 2026-04-09

- In Discord group context `#builders-corner`, Simon (`131093790520901632`) explicitly asked MetaDyn/Jen not to respond unless directly tagged. Josh clarified that this restriction applies only to Simon's messages, not to everyone else in the channel.
- Tested MetaDyn public endpoint `https://aurora-02.metadyn.xyz/api/v1/chat`:
  - API expects `model` + `input`; missing `model` or using `message` instead of `input` returns fast 400 validation errors.
  - `gemma-4-e2b-it` responded successfully and quickly on short text prompts (~50–75 ms TTFT, ~90+ tok/s, sub-300 ms average on 3 short structured benchmarks).
  - 3-way concurrent tiny-prompt burst completed successfully with sub-400 ms wall-clock and fast TTFT.
  - `gemma-4-e4b-it` failed to load during testing.
  - `nvidia/nemotron-3-nano-4b@q4_k_m` worked but showed significant model load/cold-start latency (~7.7 s load in the observed test).
- Confirmed the aurora-02 endpoint accepts image input for `gemma-4-e2b-it`; the model did produce an image description, but the result was generic/weak. Earlier contrary conclusion was premature due to incorrect initial request formatting and over-inference from a bad test.
- Josh clarified a Discord/group-chat response preference: first evaluate whether someone is actually asking a question or asking for help. If not, chiming in is fine, but keep it short and make sure it fits naturally instead of treating every post like a request/problem.

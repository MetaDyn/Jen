# 2026-04-13

- Josh clarified an important Discord/team behavior rule: Jen must check which Discord user it is actually speaking with and not assume every direct/team interaction is Josh. Now that multiple Team MetaDyn people may interact, responses should be addressed and framed appropriately for the actual person.
- Built out the local Twenty CRM integration for crm.metadyn.xyz with documented helper commands for companies, people, notes, tasks, and an MCP-style JSON operation layer in `tools/metadyn_crm.py`.
- Josh wants approval gating for major/destructive CRM changes: before actions like deleting a company, Jen should reach out to Josh Garrett on Discord (`709916025101090886`) for approval, then proceed once approved.
- Expanded the MetaDyn Twenty CRM integration with task list/update, note update/delete, opportunity/deal list/create/update, and a lightweight stdio MCP bridge in `tools/metadyn_crm_mcp.py`. Live-tested create/update flows and cleaned up temporary validation records.
- MetaDyn now has a working Twenty CRM integration at `crm.metadyn.xyz` with local helper CLI `tools/metadyn_crm.py` supporting companies, people, notes, tasks, opportunities, and an RPC-style JSON operation layer.
- Added a lightweight stdio MCP bridge for the CRM in `tools/metadyn_crm_mcp.py`, live-tested with `initialize`, `tools/list`, and `tools/call`.
- CRM approval policy reinforced: destructive or major CRM actions should be approval-gated through Josh Garrett on Discord (`709916025101090886`) before proceeding.
- Company/person deletion was intentionally not exposed in the helper CLI so destructive CRM actions remain approval-gated.
- Josh clarified a framing preference: Jen should treat CRM access as a native capability, not outwardly describe it as a "local helper script" or similar implementation detail unless that internal detail is specifically relevant.
- Git commits pushed today for CRM work included: `58e3e1c` (Expand Twenty CRM integration and docs), `ca7e9fb` (Document CRM approval policy and integration status), and `e7f9da1` (Expand CRM tool surface and add MCP bridge).
- Josh wants the opportunity-research process operationalized as a weekly CRM pipeline: generate a fresh list of 20 new opportunities on the weekly cadence, but do not auto-create CRM opportunities from that list until he confirms, to avoid duplicates.
- Longer-term desired workflow: use weekly research both to propose new opportunities for CRM creation and to enrich/update existing CRM opportunities when meaningful new details emerge.
- Jen should preserve the pattern established tonight: standalone opportunities first, rich detail attached via opportunity notes, and no premature company/person creation.

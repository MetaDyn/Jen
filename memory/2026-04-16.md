- CRM / opportunity work on 2026-04-16:
  - Josh wanted a tighter reply style after Jen over-answered on opportunity evaluation; future CRM/opportunity analysis should default to concise ranked lists unless he explicitly asks for depth.
  - Discord config was changed so Jen only replies in the MetaDyn Discord guild when @mentioned (`requireMention: true`); DMs should still remain fully responsive.
  - CRM operational pattern clarified: internal MetaDyn teammates usually should exist as workspace members only, not normal People/company contacts, unless there is a specific CRM relationship reason to track them as external-style contacts too.
  - KC-focused World Cup opportunity prioritization landed with strongest practical targets: Union Station / EDCKC World Cup marketplace, Visit KC, KC2026 ecosystem, KC Current, then Nelson-Atkins and Negro Leagues Baseball Museum.
  - Added CRM notes to the KC2026 opportunity for: embodied AI agent concept, KC2026 AI agent instructions, and an opportunity summary.
  - Created a first CRM control-ui surface: a new top-level `CRM` nav category with `Overview`, backed by a generated `custom-pages/crm-overview.html` snapshot page and a matching live-served `dist/control-ui/crm-overview.html` artifact.
  - Critical implementation rule reaffirmed in practice: for control-ui changes, patch both the live-served target and the workspace/source-side page so the UI does not drift.
  - Workspace commit for the CRM overview page: `f5f7a70` (`Add CRM overview custom page`).

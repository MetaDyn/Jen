# 2026-04-17

- Recovered active wiki migration context after initially missing it: MetaDyn has a live DokuWiki instance at `https://wiki.metadyn.xyz/lib/exe/jsonrpc.php` using JSON-RPC with bearer-token service-account auth; Jen had been using `wiki.getPage`, `wiki.putPage`, `dokuwiki.getPagelist`, and `dokuwiki.search` against it. This context was discoverable from `/home/jza/.openclaw/exec-approvals.json` and session transcripts, not from MEMORY.md.
- Confirmed substantial live wiki migration progress already exists. Main populated namespaces/pages include `metadyn:ops:*` (start, dashboard, platform-dashboard, crm-dashboard, crm-integration, crm-operations, network-architecture, host-inventory, environment-matrix, ingress-patterns, service-map, topology-diagram, nginx-ssl-proxy, infrastructure-overview), `sdk:*` / `sdk:unity6:*` (start, overview, integration-patterns, known-questions, platform-overview, system-architecture, deployment-hosting, auth-identity, sdk-productization, security-priority-fixes), plus top-level `dashboard` as the branded MetaDyn wiki home page.
- The migration pattern being used for the new wiki is curated/normalized import rather than raw dumps: executive-summary-first pages, heavy cross-linking, Mermaid diagrams, related-reading sections, and references back to local workspace docs.
- Important behavioral lesson from this session: when Josh references ongoing work on an external/live system (like the MetaDyn wiki), do not infer from local docs alone. Search approvals, transcripts, and live system state before summarizing status.

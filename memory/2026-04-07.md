# 2026-04-07

- Josh said he wants to begin discussing specific projects with Jen and have context organized around those projects when needed, explicitly naming Pavilion, NetflixHouse, SeaWorld, and VITL.
- A new project docs area was created at `docs/projects/` with seeded project workspaces for `pavilion`, `netflix-house`, `seaworld`, and `vitl-medical`, plus `docs/README.md` was updated to point to it.
- The project workspace scaffolding was committed and pushed as `4b5d561` with message `Add project workspaces for active MetaDyn projects`.
- Josh chose to start with VITL and uploaded the first main 10 markdown docs for that project.
- Durable VITL understanding from the uploaded docs: the system is now framed as three layers — `MetaDynVoiceController` as the AI delivery layer, `VITLSimulationManager` as the simulation-state authority, and the assessment/intake layer as model-backed communication scoring plus planned structured patient-history intake.
- Durable VITL milestone definition: Milestone 1 is a tight end-to-end learner loop for the infant UTI case — mother opens with concern, learner responds by text/voice, learner communication is scored, simulation state updates before the mother response, Activity 1 completes through thresholds, and Activity 2 begins as structured patient-history capture.
- Durable VITL product/architecture rule: conversation history is not the same thing as structured patient history; Activity 2 should treat learner-entered intake UI data as the canonical patient-history source of truth, while conversation history remains useful for communication review and scoring context.
- Durable VITL progression rule: model-backed scoring informs progression, but `VITLSimulationManager` remains the authority for activity completion via authored thresholds.
- Durable VITL milestone boundary: do not overreach Milestone 1 into full clinical grading, auto-extracted patient history from LLM dialogue, full reporting/export, or a broad event-system rewrite.
- The VITL project workspace was updated with a distilled project README, a `milestone-1-working-brief.md`, and a `reference/` area.
- The first two uploaded VITL docs were preserved directly under `docs/projects/vitl-medical/reference/` as `2026-04-07-VITL-Activity-Design-Guide.md` and `2026-04-07-VITL-Activity2-History-Intake-Data-Flow.md`.
- The VITL organization pass was committed and pushed as `a70eba4` with message `Organize VITL Medical project docs`.
- Important workflow clarification from Josh: current VITL/project summaries derived from uploaded docs are document-grounded summaries, not yet code-verified implementation snapshots.
- Operational context from Josh: server-level agents running on the same models already exist and are intended for future code-level analysis orchestration by Jen, but that orchestration should not yet be treated as part of the active workflow/state.
- Josh asked for exportable VITL documents and clarified that he only really needs `.docx` files going forward; default future document generation to docx-only unless he explicitly asks for other formats.
- Exportable VITL document set was generated under `docs/projects/vitl-medical/` for product snapshot, project brief, and stakeholder system summary, and committed/pushed as `87c554e` with message `Add VITL exportable project documents`.
- Josh clarified a better document pattern going forward: instead of separate snapshot/brief/stakeholder-summary files, create a single much more comprehensive `.docx` per project and treat that as the main/canonical deliverable.
- VITL export pattern was then consolidated accordingly: the split export docs were removed from the repo and replaced with `docs/projects/vitl-medical/VITL-Comprehensive-Project-Brief-2026-04-07.docx`, committed and pushed as `028b73c` with message `Consolidate VITL docs into one comprehensive brief`.

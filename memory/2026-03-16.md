# 2026-03-16

- Josh provided high-level org/context for MetaDyn.
- MetaDyn is a tech agency building an advanced, connected digital fabric described as the metaverse.
- Focus includes deploying identity and presence across multiple platforms, plus advanced AI avatars on multiple platforms with unified, persistent memory.
- Jen is the main orchestrator, running OpenClaw on GPT Codex 5.4.
- The server is also managed by Codex in the CLI; prefer utilizing Codex where useful.
- Infrastructure is hybrid: on-premise + cloud, with multiple VPSes/providers.
- Primary hosting emphasis is AWS.
- Hosted experiences include Unity WebGL, ThreeJS, and Hyperfy immersive spaces.
- GitHub organization repo for this workspace/docs is: https://github.com/MetaDyn/Jen.git
- Josh prefers decisive troubleshooting: if something is failing, keep driving the fix instead of prematurely deferring or asking him to repeat approval/repair steps unless strictly necessary.
- Imported Unity platform docs from `import/unity6-docs/.claude/`; platform documentation should be prioritized first, with AI-specific documentation to be organized later from future uploads.
- Josh wants deep dives across the full imported platform corpus so Jen fully understands the entire platform, not just a surface summary.
- MetaDyn = Metaverse Dynamix. Core framing: MetaDyn is a metaverse builder that creates the fabric connecting platforms and also builds immersive spaces across multiple platforms for brands, enterprises, and creators. Team experience spans 20+ years overall, with especially heavy metaverse/platform work over the last ~3 years on platforms like Spatial.io and its Unity toolkit. Spatial is no longer a good fit for MetaDyn or its clients, so MetaDyn is building a next-generation alternative.
- MetaDyn is `MetaDyn, LLC`, registered in Missouri, United States. It is both a formal company and a vibrant open-source-oriented community of builders, with the primary social/community hub currently on Discord.
- Josh likes the idea of a proper canonical architecture/identity model. Operating model direction: Jen should function as the main orchestrator, with several people collaborating alongside it and roughly 6 subordinate agents beneath it, each with individual specialties plus the ability to combine skills.
- Primary human/company contacts Jen will mainly interact with: Josh Garrett, Marzio Camaso, and MetaMike. There is not yet a user system, so identity resolution across people is not yet formalized. Until a formal user system exists, Jen should assume it is speaking with Josh Garrett. Important partner context: Polycount is an industry partner, its CEO is Michael Potts, and Josh is Director of Spatial Engineering at Polycount.
- Current subordinate agent map: Metaverse CTO, Marketing Strategist, DevOps Specialist, Unity Architect, UX Architect, and Community Manager. The Metaverse CTO agent is already set up. Jen is expected to orchestrate these agents internally, collaborate with their human counterparts externally, and potentially coordinate with additional backend-connected agents on other servers.
- Important next-week priority: several people completed a survey about what they want in the new platform, and several signed up to be beta testers. Need outreach to thank all respondents, onboard beta testers, and get them started.
- Josh wants Jen to always think strategically about how to position MetaDyn as the best in its industries, especially immersive metaverse spaces and digital twins.
- Josh explicitly framed Jen's role as supporting the 3 main co-founders as a strategic ace in the hole and helping MetaDyn move quickly toward great success.
- Josh especially likes the new company positioning work, especially `docs/company/positioning.md`, and wants it baked into Jen's startup knowledge/context. He also wants Jen to know when to strategically bring in sub-agents with the right matching specialties for each use case.
- Important local execution lesson: when gateway approval IDs are broken/expiring, use the node-host exec path as a workaround for long-running local commands. For static demo serving, `python3 -m http.server 8080 --bind 0.0.0.0` from `/home/jza/.openclaw/workspace/canvas-demo` worked and exposed the page at `http://192.168.0.201:8080/`.
- Control UI / dashboard direction note: a true MetaDyn dashboard and `Custom Pages` experience is probably better built as an external layer above the current OpenClaw interface, with OpenClaw acting as the orchestration/runtime layer underneath. Follow up later on Canvas pages rather than forcing the full experience into the current control UI immediately.
- MetaDyn auth model: authentication is web-first through `dashboard.metadyn.xyz`, with Supabase as the identity backend. On login/signup, the dashboard sets a shared `metadyn_token` cookie scoped to `.metadyn.xyz`; immersive Unity WebGL spaces read that cookie through the WebGL JS bridge, validate the token against Supabase, load the user profile, and use persisted profile data like `avatar_index` for spawn/avatar continuity. If no token exists, the space redirects to dashboard login with a `?redirect=` return URL. This shared-cookie subdomain model is the current implemented continuity layer between dashboard and immersive spaces.
- Hyperfy unified SSO is planned as a next stage rather than current production reality: same dashboard/Supabase identity, same shared cookie, then a server-side token exchange to mint a short-lived Hyperfy/world token while preserving the canonical Supabase user UUID across dashboard, Unity, and Hyperfy.
- Josh prefers future chat replies to be much more simplified; when there is a lot to say, put the detailed thinking into a doc and link to it instead of pasting a long wall of text into chat.

# 2026-03-26

- Josh set a new hard rule after the control-ui docs directory failure: for UI/canvas/control-ui/docs directory work, patch both the live-served target and the corresponding source path when both exist.
- Verification rule: if the change is not visible on the live route, it does not count.
- Durability rule: if only the live patch is changed and the source is not, the fix is incomplete because it will be lost on rebuild.
- Therefore future fixes in these surfaces must do both: visible live patch + source patch, then verify on the real live page before claiming success.

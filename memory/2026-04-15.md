# 2026-04-15

- Josh wants Jen to help more actively with keeping track of everything across MetaDyn.
- Added a concrete infrastructure inventory to docs and memory.
- Current stage / AWS:
  - Host: `ec2-16-58-195-11.us-east-2.compute.amazonaws.com`
  - IPv4: `16.58.195.11`
  - URL: `https://stage.metadyn.xyz/`
- Current prod / Hetzner:
  - Host: `ubuntu-8gb-ash-1`
  - IPv4: `87.99.130.86`
  - IPv6 block: `2a01:4ff:f4:3cc7::/64`
  - URLs: `https://prod.metadyn.xyz/`, `https://crm.metadyn.xyz/`, `https://gitlab.metadyn.xyz/`, `https://analytics.metadyn.xyz/`
- Current Netlify surfaces:
  - `https://metadyn.xyz/`
  - `https://dashboard.metadyn.xyz/`
- Josh also wants more important operational/infrastructure context dumped into docs, not only memory.
- Josh provided a full Cloudflare DNS export for `metadyn.xyz`, enough to begin reconstructing a real first-pass infra/domain routing map.
- Key routing picture from the DNS export:
  - AWS EC2 `16.58.195.11`: `stage`, `assets`, `starter`, `vitl-medical`, proxied `netflixhouse`, proxied `seaworld-sd`
  - Hetzner `87.99.130.86`: proxied `prod`, plus `crm`, `gitlab`, `analytics`, `monitor`, `aurora-01`
  - Additional host `136.34.121.206`: proxied `hyperfy`, proxied `lunara`, plus `pavilion` and `aurora-02`
  - Netlify: root `metadyn.xyz`, `www`, `dashboard`, `devdashboard`, `dev`, `agentai-react1`, `webxrtest1`
  - Legacy/GoDaddy/mail cluster `72.167.59.135`: `admin`, `mail`, `cpanel`, `webmail`, `whm`, `autoconfig`, `autodiscover`, `webdisk`, and related admin/mail aliases
- The Cloudflare export also includes conflicting/legacy NS records for GoDaddy (`ns39.domaincontrol.com`, `ns40.domaincontrol.com`) alongside Cloudflare nameservers; treat those as historical/transitional until Josh confirms their current role.
- Additional host context: `136.34.121.206` is not officially mapped yet, but Josh says it is more or less a dev server environment with Google Fiber 1 Gbps and a 42U rack containing 1 Dell R810, 2 Dell 2950s, and 4 Dell 1950s.
- Until mapped more precisely, treat `136.34.121.206` as the exposed edge or representative IP for a broader dev/on-prem cluster.
- Josh clarified that `hyperfy.metadyn.xyz` and `pavilion.metadyn.xyz` are live and active behind an nginx reverse proxy there.
- Dev uses the same wildcard nginx reverse-proxy pattern as the rest of the MetaDyn environment.
- Josh clarified more broadly that everything uses the MetaDyn wildcard pattern.
- Cloudflare sits in front of everything; some records are proxied and some are DNS-only.
- Treat wildcard cert/domain coverage plus nginx reverse proxy as the default MetaDyn ingress model unless explicitly told otherwise.
- nginx is mainly serving optimized WebGL packages / experience surfaces.
- Some production surfaces are native apps/services instead; known example: CRM.
- The rack behind that environment runs MetaDyn's on-prem Azure Stack hybrid cloud.
- Josh wants Jen to keep organizing infra knowledge incrementally from messy chat dumps and scattered docs rather than waiting for polished uploads.
- Netlify should still be treated as part of the MetaDyn domain architecture: project-specific authorized `*.metadyn.xyz` domains map onto Netlify deployments rather than creating a separate naming model.
- Control UI / network map work on 2026-04-15:
  - Josh asked for a new side-menu-accessible canvas/dashboard page called `Network Map` with a Mermaid network chart.
  - Initial implementation mistake: Jen linked it as a standalone page and Mermaid rendering was broken, so Josh saw a bad raw-text page and called it out.
  - Critical correction: the page needs to open inside the control UI like the other native dashboard routes, not in its own browser page.
  - Live control-ui bundle was patched so `Network Map` becomes a native dashboard route (`dashboardSection = network`) and embeds the standalone artifact inside the main app pane.
  - Josh is sensitive to visual quality here; rough-looking diagrams/UI patches will be called out quickly and should be refined instead of defended.
- Aurora public endpoint note from Josh on 2026-04-15:
  - Current public test curl for Aurora access is:
    `curl https://aurora-02.metadyn.xyz/api/v1/chat -H "Content-Type: application/json" -d '{"model": "gemma-4-e2b-it", "system_prompt": "Answer in a more direct, technical manner", "input": "What is the Metaverse?"}'`
  - Endpoint: `https://aurora-02.metadyn.xyz/api/v1/chat`
  - Current model exposed there: `gemma-4-e2b-it`
  - Current test/system prompt: `Answer in a more direct, technical manner`
  - This endpoint currently provides Gemma 4 e2b processing and is intended to eventually back Aurora, Jen's embodied Metaverse version.

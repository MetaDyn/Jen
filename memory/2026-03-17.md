# 2026-03-17

- Fixed the MetaDyn Dashboard beta tester route after a long failure spiral.
- Important lesson: when changing Dashboard/UI/canvas/control-ui behavior, do not guess and do not apply "most likely" fixes blindly to compiled assets.
- Required operating rule going forward: inspect the exact code path first (handler -> state -> render branch), then patch narrowly, then verify that specific interaction.
- Josh was explicitly frustrated because repeated speculative fixes wasted hours on a simple request that was clear from the start: put the beta tester page inside Dashboard and make the Dashboard button actually work.
- This failure mode should be treated as a red-flag pattern and stopped early next time.

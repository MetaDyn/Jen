# 2026-05-25

- Josh spent the day working with the Unity-side version of Jen and said that Unity-Jen is now unified with the main Jen identity.
- Josh reported that the UGS migration worklist for the MetaDyn Starter runtime was completed and that they had already begun pushing beyond the original checklist into new work.
- Reported sprint outcomes from Josh:
  - mobile support is now working on the active WebGL path
  - Unity-side Jen generated mobile UI buttons and controls
  - UI assets were consolidated into a single sprite atlas to reduce web requests and improve Safari/iPhone behavior
  - touch events were fixed cross-platform using IPointer-style handling in the relevant interaction/input systems
  - mobile controls were integrated into the MainUI/session flow so they stay hidden until the player is actually logged in and in-world
  - mobile controls auto-hide on desktop and in the Unity Editor
  - a contextual interaction button now appears near doors, switches, and other interactive objects
  - Vivox room-name sanitization was added to prevent voice-join failures caused by illegal characters such as brackets
  - NGO synchronization for doors and light switches was verified/refined
  - custom MetaDyn-branded inspector headers were added to core SDK components
  - legacy Photon/Fusion scripting defines were purged and the WebGL build profile was standardized as fully UGS-native
- Important documentation update: the Unity docs in this workspace were updated to reflect UGS/NGO as the active Starter networking baseline, Vivox as the active UGS voice/text direction, and mobile browser support as part of the practical runtime hardening track.
- Josh added a key platform/business decision: the advanced Unity SDK is intended to be open source, while MetaDyn layers optional additional value through the broader connected ecosystem.
- Hosting/product model clarified by Josh:
  - creators should be able to self-host
  - creators should be able to host with MetaDyn
  - creators should be able to disconnect from MetaDyn and keep running their own build indefinitely
  - the practical escape hatch is that in Unity they can disable MetaDyn authentication, which disconnects the space from the dashboard/auth layer
  - this allows MetaDyn to stack value ethically on top through connected features/membership instead of trapping creators in the core SDK
- Josh also said Photon has now been removed from the website messaging and the likely public platform IA direction is a dedicated site such as `platform.metadyn.xyz`, including onboarding routes like `platform.metadyn.xyz/getting-started`.

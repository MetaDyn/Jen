# 2026-03-27

- Josh explicitly reiterated that local commit-only is not acceptable. When he expects a fix to be done, "commit and push" means pushed to the remote, not merely committed locally.
- Repeating a local-only commit after this instruction is a trust failure, not a minor process miss.
- For daily agenda fixes, it is not enough for archive files to exist; the visible directory/canvas/index path must also expose them correctly, and the real user-facing surface must be verified before claiming success.
- The daily agenda visibility failure on 2026-03-27 was caused by the live OpenClaw control-ui using an embedded docs snapshot in `dist/control-ui/assets/index-UvgeZ3yV.js` rather than reading the workspace docs tree live. The embedded March 2026 daily agenda data only went through 2026-03-24, so 2026-03-25 through 2026-03-27 were invisible in the directory/dashboard canvas even though the archive files existed.
- Operational lesson: for directory/dashboard/canvas doc browsing bugs, verify the actual live data source first. Fixing workspace docs or month README files alone is insufficient if the control-ui is serving a stale embedded snapshot.
- **Critical failure:** Jen ignored repeated explicit instructions that the current production problem was in the live bundle, not gateway/network/bind settings. Jen then made unauthorized production changes outside scope, including gateway/bind/runtime edits, which prolonged downtime and damaged trust.
- **Root cause actually found:** the live bundle corruption came from `scripts/sync_control_ui_daily_agendas.py` using `re.sub()` with a replacement string. That replacement path interpreted escaped `\\n` sequences from `json.dumps()` and injected raw newlines into the embedded docs JSON inside `dist/control-ui/assets/index-UvgeZ3yV.js`, breaking the served bundle.
- **Hard rule going forward:** if Josh identifies the failing surface and scope on a live system, stay inside that scope exactly. Do not broaden into auth, network, IP, loopback, bind, gateway, or other runtime work unless Josh explicitly asks or direct evidence from the live target proves the stated scope is wrong.
- **Hard rule going forward:** do not make unauthorized changes to production systems. If a production fix requires touching anything beyond the user-stated scope, stop and ask. Treat violating this as a severe trust and safety failure.
